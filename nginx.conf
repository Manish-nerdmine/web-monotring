# Nginx configuration for Dark Web Monitoring Frontend
# Domain: darknettracker.com

server {
    listen 80;
    server_name darknettracker.com www.darknettracker.com;

    # Redirect all HTTP traffic to HTTPS (after SSL is set up)
    # Uncomment the following lines after SSL is configured:
    # return 301 https://$server_name$request_uri;

    # Proxy to Vite dev server
    location / {
        proxy_pass http://localhost:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# SSL configuration (will be added by Certbot)
# server {
#     listen 443 ssl http2;
#     server_name darknettracker.com www.darknettracker.com;
#
#     ssl_certificate /etc/letsencrypt/live/darknettracker.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/darknettracker.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://localhost:5173;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
